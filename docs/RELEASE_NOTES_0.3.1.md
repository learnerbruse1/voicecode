# VoiceCode v0.3.1 — 稳定性与兼容性修复版

发布日期：**2026 年 8 月 10 日**

VoiceCode v0.3.1 是修复版：解决 GPU 转写失败后录音卡死的问题，并加固了不同配置电脑上的健壮性与流畅度。

## 下载

### Windows x64 安装程序

`VoiceCode-v0.3.1-Windows-x64-Setup.exe`

- 文件大小：`125,338,286` 字节
- SHA-256：

```text
F5C24EBE2D3B6A3A23E7776B17FE0774FB3C0AB22A49E0ED3B44F75396DC71FA
```

> **重要提示：** 当前构建尚未进行 Authenticode 代码签名，Windows 可能显示 SmartScreen 或“未知发布者”提示。正式发布前签名后，文件大小和 SHA-256 会变化，请重新生成校验值。

## 主要修复

- 修复 GPU 转写出错/卡死后录音按钮与应用整体冻结：转写改为串行工作线程并加 120 秒看门狗超时，原生推理不再长期持有模型锁；出错或超时自动弃用坏模型并回退 CPU int8，无需重启即可恢复。
- 关闭「处理中」遮罩现在会中止在途请求并复位界面，按钮不再卡死。
- 模型预热（warm-up）有界（30 秒）且不再持锁，避免启动或切换模型时卡死。
- 前端状态/统计/实时草稿轮询增加超时保护。
- CPU 线程默认值加固（`WHISPER_CPU_THREADS` 封顶到逻辑核心数）；首启推荐模型按显存动态选择（低显存 GPU 推荐 `base`）。
- 本版包含 v0.3.0 的全部新功能：faster-whisper 1.2.x、日语特化 Kotoba Whisper v2.0、Distil Whisper Large v3.5、模型选择按钮三语提示。

## 验证

- 自动化验证通过：12 个必需文件、嵌入式 pip 26.2.1、三语目录各 394 条、扫雷载荷、静默卸载。
- 手动冒烟通过：启动、`/health` PID、版本 meta 0.3.1、`/models` 返回全部 9 个模型、tiny 模型 CPU int8 转写 HTTP 200。
- 本构建未签名；正式发布仍需时间戳代码签名与签名后复验。